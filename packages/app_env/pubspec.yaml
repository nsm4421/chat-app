name: app_env
description: Platform-scoped environment configuration for domodachi.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  envied: ^1.3.3

dev_dependencies:
  lints: ^5.1.1
  build_runner: ^2.13.0
  envied_generator: ^1.3.3
